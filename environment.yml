name: cloud
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - kubectl
  - openssl
  - curl
  - wget
  - make
  - git
  - pip
  - pip:
      - pyyaml

# Install the real k3d CLI separately if it is not already available:
#   curl -s https://raw.githubusercontent.com/k3d-io/k3d/main/install.sh | bash
